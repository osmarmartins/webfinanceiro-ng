export interface AccessToken {
    access_token: string;
    token_type: string;
    expires_in: string;
    scope: string;
    idUsuario: number;
    nome: string;
    email: string;
    perfil: string;
}
